services:
  gameserver:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env.dev
    volumes:
      - ${DATA_PATH}:/gameserver/data

    ports:
      - "${PORT}:${PORT}"