syntax = "proto3";

package packets;

option go_package = "/pkg/packets";

// General

message PingMessage {
  uint64 timestamp = 1;
}

message ChatMessage {
  string msg = 1;
}

message IdMessage {
  uint64 id = 1;
}

// Authentication

message LoginRequestMessage {
  string username = 1;
  string password = 2;
}

message RegisterRequestMessage {
  string username = 1;
  string password = 2;
}

message OkResponseMessage {}

message DenyResponseMessage {
  string reason = 2;
}

// Lobby messages

message LobbyPlayerData {
  uint64 client_id = 1;
  string username = 2;
  bool is_ready = 3;
}

message LobbyData {
  uint64 id = 1;
  string name = 2;
  int32 current_players = 3;
  int32 max_players = 4;
}

// List

message ListLobbiesRequest {
  // Pagination
  int32 page = 1;
  int32 page_size = 2;

  // Filters
  string name = 3;
  string format = 4;
  int32 state = 5;
}

message ListLobbiesResponse {
  int32 count = 1;
  repeated LobbyData lobbies = 2;
}

// Create

message CreateLobbyRequest {
  string name = 1;
  string format = 2;
  int32 max_players = 3;
  bool is_private = 4;
  optional string password = 5;
}

message CreateLobbyResponse {}

message JoinLobbyRequest {
  uint64 lobby_id = 1;
  optional string password = 2;
}

message JoinedLobbyResponse {
  uint64 lobby_id = 1;
  string lobby_name = 2;
  string host_username = 3;
  repeated LobbyPlayerData players = 4;
}

message PlayerJoinedLobbyResponse {
  LobbyPlayerData player = 1;
}

// Entry
message Packet {
  uint64 sender_id = 1;
  oneof msg {
    // General messages
    PingMessage ping = 2;
    ChatMessage chat = 3;
    IdMessage id = 4;

    // Authentication messages
    LoginRequestMessage login_request = 5;
    RegisterRequestMessage register_request = 6;
    OkResponseMessage ok_response = 7;
    DenyResponseMessage deny_response = 8;

    // Lobby messages
    ListLobbiesRequest list_lobbies_request = 9;
    ListLobbiesResponse list_lobbies_response = 10;

    CreateLobbyRequest create_lobby_request = 11;
    CreateLobbyResponse create_lobby_response = 12;

    JoinLobbyRequest join_lobby_request = 13;
    JoinedLobbyResponse joined_lobby_response = 14;
    PlayerJoinedLobbyResponse player_joined_lobby_response = 15;

    // Game messages
  }
}
